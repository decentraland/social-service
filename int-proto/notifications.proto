syntax = "proto3";

package decentraland.social.notifications;

import "friendships.proto";

// For internal use only (event notifications)
message Event {
  decentraland.social.friendships.FriendshipEventResponse friendship_event = 1;
  string from = 2;
  string to = 3;
}
